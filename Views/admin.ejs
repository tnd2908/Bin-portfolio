<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include('layouts/header'); %>
    <link rel="stylesheet" href="/css/admin.css">
    <title>Document</title>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-2">
                <%- include('layouts/menu-bar'); %>
            </div>
            <div class="col-10">
              <div class="container-fluid pt-5">
                <div class="row">
                  <% 
                  for (let index = 0; index < projects.length; index++) {
                      %> 
                        <div class="col-xl-3 col-lg-4 col-md-6 col-6">
                          <div class="bg-dark project rounded" style="padding: 15px;">
                            <img src=<%= projects[index].thumb %> alt="" class="thumb">
                            <div class="d-flex">
                              <h5 class="mt-3 mb-3"> <%= projects[index].name %> </h5>
                            </div>
                            <div class="d-flex justify-content-between">
                              <button id="btn" class="btn btn-edit"> <i class="fal fa-pen me-2"></i>Chỉnh sửa </button>
                              <button onclick="console.log(<%= JSON.stringify(projects[index]._id) %>)" class="btn-delete"> <i class="fal fa-trash-alt me-2"></i> Xoá </button>
                            </div>
                          </div>
                        </div>
                      <%
                    }
                   %>
                </div>
              </div>
            </div>
        </div>
    </div>
</body>

</html>