<!DOCTYPE html>
<html lang="en">
    <%- include('layouts/header'); %>
    <title>Viral</title>
<body>
    <%- include('layouts/Navbar'); %>
    <div class="container-fluid mb-5">
        <div class="row">
            <div class="col-12">
                <h1 class="fs-1 text-center mt-5"><%= detail.name %></h1>
            </div>
        </div>
        <div class="container d-flex justify-content-center">
            <iframe class="mt-5" width="100%" height="100%"  src=<%= detail.youtubeUrl%>
            frameBorder="0"
            allowFullScreen
            ></iframe>
        </div>
    </div>
</body>
</html>